# 📝 更新日志 (Changelog)

All notable changes to the **Antigravity Enhance Tools** project will be documented in this file.

---

## [v0.1.5] - 2026-09-23

### 多账号原生动态管理体系（对比 v0.1.4 深度重构）
- **原生内嵌化融合设计**：对比以往需单独启动外部独立工具的臃肿操作，本次将多账号切换与配额管理全面移植并原生内嵌至 Antigravity 底栏，实现零额外进程常驻与无缝内联交互；
- **无上限动态账号池**：重构底层账号管理模型，彻底告别静态绑定，支持任意数量 Google 账号（PRO / ULTRA / FREE）的无上限添加、动态关键词即时搜索、分类标签筛选与独立配额进度条渲染；
- **全账号实时云端配额同步**：彻底突破旧版仅能展示切换历史快照的限制，打开多账号面板或点击单个卡片刷新时，主进程将自动通过本地保存的 OAuth 凭证并发向 Google 官方云端端点静默同步所有账号的最新配额与重置倒计时，重置时间已过自动恢复为 100% 并提示“刚刚已重置”，换号前余量一目了然；
- **JWT 签名验真与防污染隔离**：重构凭证注入与写入逻辑，加入 JWT 签名邮箱验真机制，确保每组凭据在本地私有目录（`~/.gemini/account_profiles/<safe_email>/`）中严格物理隔离，彻底解决跨账号切换时出现的凭证串号与状态污染；
- **退出登录二次确认与深度注销**：在账号管理面板中新增退出登录交互，提供优雅的防误触二次确认弹窗；确认退出后通过底层 API 彻底擦除 Windows Credential Manager / macOS Keychain 中的系统凭证，杜绝旧凭据残留；
- **本地安全隐私保障声明**：所有账号数据、Token 凭证 100% 仅保存在用户本地设备，零云端中转服务器，认证均直接与 Google 官方端点通信。

### 底栏 UI 与交互体系全新升级（对比 v0.1.4 视觉与布局重塑）
- **头像与账号切换合二为一**：对比 v0.1.4 底栏头像与切换按钮分离占位的设计，本次将头像、用户名与切换入口彻底重构为一体化交互卡片，点击头像或昵称即可直接呼出多账号管理弹窗；
- **图二同款高质感悬浮指示窗 (Tooltip)**：全面推翻旧版简陋的气泡提示，像素级重构为现代化微光深色毛玻璃圆角悬浮面板，文字间距、内边距与阴影层叠精细调优，鼠标移入顺滑渐入、移出平滑渐隐；
- **底栏控件防挤压排版**：优化底栏配额胶囊与周边状态栏图标的几何间距，彻底解决小屏或窄窗口下的元素重叠与挤压错位。

### 快捷键与稳定性深度加固（对比 v0.1.4 修复与防卡死升级）
- **修复 `Ctrl + I` 无法打开问题**：深入排查并清除了全局快捷键监听冲突与事件穿透，确保代码行内编辑（Inline Edit / Composer）快捷键精准响应；
- **运行时防假死双重保障**：加固 DOM 缓存守卫（Cache Guard）与并发防重入锁，在多账号频繁切换或网络握手超时等极端场景下，杜绝 Electron 渲染进程陷入事件死循环或无响应。

### 全景汉化深度扩充（对比 v0.1.4 词库大版本扩容）
- 新增 **437 条** 核心界面汉化词条，全面覆盖多账号管理模态框、二次确认弹窗、高级系统偏好、网络重试状态与模型微调提示语，汉化覆盖率与母语化水准显著提升。

### 技能中心 (Skills Hub) 架构与安装生态扩充
- **新增「📦 其他」智能兜底分页**：新增第 6 大技能分类 Tab，若用户新安装的技能未命中预设的 5 大分类规则，将自动无感归入「其他」分区，支持独立筛选与卡片数量动态计数；
- **原生自带 vs 用户添加 双标签透传**：在各技能卡片右上角新增来源分类标识，原生官方自带标注为「原生自带」（淡紫蓝标），用户自定义或从外部引入的技能明确标注为「用户添加」（翡翠绿标），插件扩展标注为「插件扩展」（专属紫标），来源一清二楚；
- **一键从 GitHub 自动安装与部署**：技能中心底部新增「➕ 从 GitHub 添加技能」按钮，用户只需粘贴任意 GitHub 技能仓库链接，系统将自动发起全新对话会话、自动注入专业部署提示词，并指导 Gemini 完成规范解析、环境依赖适配以及本地 `~/.gemini/config/skills/` 的部署。

### 构建工具链与交付套件升级
- `Antigravity Enhance Tools.exe` 升级至 v0.1.5，体积流线化优化，启动探测与注入速度显著提高；
- 同步更新生成 `Antigravity-Enhance-Pack.zip` 与 `Antigravity-Enhance-Pack.tar.gz` 双端完整离线包。

---

## [v0.1.4] - 2026-09-21

### 技能中心 (Skills Hub) 体验全面升级
- 排版重构为更整齐舒适的 3 列响应式网格布局，卡片层次分明；
- 文字对比度大幅加深优化，亮色/暗色双主题下描述文字清晰锐利，告别发淡；
- 侧边栏技能库新增「📖 规范目录」一键打开 Windows 资源管理器对应文件夹；
- 新增「💬 立即调用」一键自动跳转新会话并装填技能；
- 支持技能来源标签（官方自带 / 用户配置 / 插件扩展）与个人专属备注。

### 启动缩放异常与防爆安全门禁
- 彻底根治因误触或 Chromium 历史缓存导致的客户端每次启动界面巨大缩放问题；
- 新增全局缩放快捷键：`Ctrl + 0` 一键重置为 100% 原始大小，`Ctrl + =` / `Ctrl + -` 顺滑缩放并带实时气泡提示。

---

## [v0.1.3] - 2026-09-21

### 跨平台与多端原生适配
- 全面支持 **macOS**（全面适配 Apple Silicon M1~M4 及 Intel 芯片，深度兼容 `/Applications/Antigravity.app` 应用结构）；
- 全面支持 **Linux** 各大主流发行版（Ubuntu、Debian、Fedora、Arch、CentOS 及 Flatpak 目录结构）；
- 新增全自动化跨平台终端安装向导 `install.sh`（支持 `curl ... | bash` 终端一行命令静默部署）与还原向导 `uninstall.sh`；
- 研发跨平台运行时智能探针：若宿主未配置独立 Node.js，自动无感复用客户端内置 Electron 引擎作为 Node 运行环境；
- 自动编译构建并发布专为 Unix 权限体系优化的 `Antigravity-Enhance-Pack.tar.gz` 离线压缩包；
- Windows 端 GUI 同步升级版本标识至 `v0.1.3`。

---

## [v0.1.2] - 2026-09-21

### 界面与交互
- 重构 GUI 安装器为现代化圆角卡片视窗，全屏控件统一应用平滑圆角设计；
- 增加右上角「浅色明亮 / 深色极夜」双主题一键实时无缝切换；
- 优化 ClearType 高清文字排版与抗锯齿渲染，彻底解决半透明窗口下的字体发虚模糊；
- 重构复选框几何向量，实现对号绝对居中对齐；

### 核心引擎
- 新增安装路径智能手动重新搜索按钮，保留原有手动浏览文件夹功能；
- 增强补丁部署与还原时的互斥进程识别与释放机制。

---

## [v0.1.1] - 2026-09-20

### 交互与功能增强
- 增加模型面板「思考能力 4 挡调节滑块（关闭 / 低 / 中 / 高）」；
- 引入底栏五段式真实上下文 Token 动态遥测与悬浮卡片；
- 接入侧边栏实时额度轮询看板；

### 稳定性与性能
- 研发独创 DOM 缓存守卫（Cache Guard），彻底解决 `MutationObserver` 死循环导致的客户端卡死与闪退。

---

## [v0.1.0] - 2026-09-19

### 首发版本
- 实现了 Antigravity 官方桌面客户端的全界面原生深度汉化；
- 实现了基于 C# WPF 的独立安装引导程序；
- 支持原生 asar 解包、语法树分析、注入与无损还原。
